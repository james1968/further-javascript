
var list = new NoteList();
var note = new Note('Test text entry for the first note');
var note1 = new Note('second test for the second note');
var noteListView = new NoteListView();
var sview = new SingleNoteView();

list.addNote(note);
list.addNote(note1);
